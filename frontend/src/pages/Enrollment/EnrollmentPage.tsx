import styles from '../../styles/pages/Enrollment/_enrollmentPage.module.scss';
import Header from '../../components/Header';

const EnrollmentPage = () => {
  return (
    <div className={styles.enrollment_page}>
      <Header />
    </div>
  );
};

export default EnrollmentPage;
